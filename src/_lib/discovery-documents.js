function escapeXml(value = "") { return String(value).replaceAll("&", "&amp;").replaceAll("<", "&lt;").replaceAll(">", "&gt;").replaceAll('"', "&quot;").replaceAll("'", "&apos;"); }
export function canonicalItems(items = []) { return [...items].filter((item) => item.url && !item.data?.robots?.includes("noindex") && !item.data?.targetUrl && !item.url.startsWith("/categories/") && !item.url.startsWith("/legacy/")).sort((a, b) => a.url.localeCompare(b.url)); }
export function renderSitemap(items, siteUrl) { const urls = canonicalItems(items).map((item) => `  <url><loc>${escapeXml(new URL(item.url, siteUrl).href)}</loc>${item.date ? `<lastmod>${new Date(item.data?.updated || item.date).toISOString()}</lastmod>` : ""}</url>`).join("\n"); return `<?xml version="1.0" encoding="UTF-8"?>\n<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">\n${urls}\n</urlset>`; }
export function renderFeed(items, site, title, description) { const entries = [...items].sort((a, b) => b.date - a.date).map((item) => `<entry><title>${escapeXml(item.data.title)}</title><link href="${escapeXml(new URL(item.url, site.url).href)}"/><id>${escapeXml(new URL(item.url, site.url).href)}</id><updated>${new Date(item.data.updated || item.date).toISOString()}</updated><summary>${escapeXml(item.data.summary || "")}</summary></entry>`).join("\n"); const updated = items.length ? new Date(Math.max(...items.map((item) => new Date(item.data.updated || item.date).valueOf()))).toISOString() : new Date(0).toISOString(); return `<?xml version="1.0" encoding="UTF-8"?>\n<feed xmlns="http://www.w3.org/2005/Atom"><title>${escapeXml(title)}</title><subtitle>${escapeXml(description)}</subtitle><link href="${escapeXml(site.url)}"/><id>${escapeXml(site.url)}</id><updated>${updated}</updated>${entries}</feed>`; }
