# Archived website

This folder preserves the website implementation and content that existed before the August 2026 reset.

It is reference material only. The active Eleventy and Tailwind configurations explicitly exclude `_archive`, and no archived tests run as part of the current project.

The former Git submodules are no longer included. Their upstream repositories are linked from `sources/README.md`.
